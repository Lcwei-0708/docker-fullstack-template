global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Metrics are pushed by Alloy (docker-stats). Keep a self-scrape so
# Prometheus health is visible in Grafana if needed.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
